# URL the SSR server uses to reach the API (server-side fetches, no CORS involved)
API_URL=http://localhost:5000/api

# URL the browser uses to reach the API after hydration (must be public)
VITE_API_URL=http://localhost:5000/api

PORT=3000
NODE_ENV=development